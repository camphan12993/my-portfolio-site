# Project slash commands

Custom slash commands for this project go here as Markdown files, e.g.
`.claude/commands/deploy.md` becomes `/deploy`. See Claude Code docs for the
frontmatter format (`argument-hint`, `allowed-tools`, `description`).

No project-specific commands are defined yet.
