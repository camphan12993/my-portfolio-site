---
import SectionHeading from '../SectionHeading.astro';
import skills from '../../data/skills.json';
---

<section id={skills.id} class="border-b border-line px-8 py-20 sm:px-12">
	<SectionHeading tag={skills.tag} icon={skills.icon} heading={skills.heading} />

	<div class="mt-10 space-y-10">
		{
			skills.groups.map((group, gi) => (
				<div>
					<p
						class="inline-flex items-center gap-2 text-xs font-medium uppercase tracking-widest text-muted"
						data-aos="fade-up"
						data-aos-delay={gi * 60}
					>
						<span class="h-1.5 w-1.5 rounded-sm bg-accent" />
						{group.label}
					</p>

					<div class="mt-6 grid gap-x-8 gap-y-5 sm:grid-cols-2">
						{group.items.map((item, i) => (
							<div data-aos="fade-up" data-aos-delay={i * 60}>
								<p class="text-sm text-ink">{item.name}</p>
								<div class="mt-2 h-1.5 w-full overflow-hidden rounded-full bg-panel-2">
									<div class="h-full rounded-full bg-ink/70" style={`width: ${item.percent}%`} />
								</div>
							</div>
						))}
					</div>
				</div>
			))
		}
	</div>
</section>
